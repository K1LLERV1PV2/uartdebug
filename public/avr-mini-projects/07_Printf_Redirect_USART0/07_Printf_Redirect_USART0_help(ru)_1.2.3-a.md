# 07_Printf_Redirect_USART0
Перенаправление printf в UART0

## File Version
Версия файла

Version 1.2.3-a

## Short Project Description
Краткое описание проекта

Перенаправление вывода `printf()` в UART0 для блокирующей передачи форматированного текста.

## Full Mini-Project Description
Полное описание мини-проекта

Этот мини-проект инициализирует UART0 и перенаправляет в него стандартный поток C `stdout`.

После перенаправления текст, формируемый функцией `printf()`, передаётся через UART0. В примере каждые 500 мс передаётся увеличивающийся счётчик:

```text
Counter = 0
Counter = 1
Counter = 2
...
```

В проекте используется блокирующая передача UART. Каждый символ, сформированный `printf()`, передаётся функции `USART_PrintChar()`, которая ожидает готовности буфера USART принять следующий байт, а затем записывает символ в `USART0.TXDATAL`.

Формат UART:

```text
115200 baud, 8 data bits, no parity, 1 stop bit
```

Обычная запись:

```text
115200 8N1
```

Получаемый текст можно наблюдать в разделе UART сайта **uartdebug.com**.

## Hardware Requirements and Setup
Требования к оборудованию и подключение

В предполагаемой схеме используются два адаптера USB-UART:

- один адаптер для программирования по UPDI;
- второй адаптер для приёма и отображения текста UART0, формируемого `printf()`.

Подключение второго адаптера:

```text
Microcontroller PB2 / TxD  ->  USB-UART RxD
Microcontroller GND        ->  USB-UART GND
```

Микроконтроллер и принимающий адаптер USB-UART должны использовать одинаковый уровень логического напряжения.

Для микроконтроллеров tinyAVR 2-series в корпусах SOIC-14 сигнал `PB2` находится на выводе 7.

Вывод `RxD` микроконтроллера в этом мини-проекте не используется.

## Quick Start
Быстрый запуск

### Hardware Connection
Подключение оборудования

Подключите первый адаптер USB-UART к микроконтроллеру для программирования через интерфейс UPDI.

Подключите второй адаптер USB-UART к микроконтроллеру для наблюдения текстового вывода UART0.

### Port Selection
Выбор портов

#### UPDI Programmer Adapter Port Selection
Выбор порта адаптера-программатора UPDI

В одной вкладке браузера откройте раздел AVR сайта **uartdebug.com** для программирования микроконтроллера.

На рисунке показано, что сначала порт не выбран.

![472](Pasted%20image%2020260726152153.png)

После нажатия кнопки `Disconnected` появится окно выбора порта.

Выберите порт первого адаптера, подключённого к интерфейсу программирования микроконтроллера UPDI.

![387](Pasted%20image%2020260726152302.png)

Если адаптер правильно подключён к микроконтроллеру и выбран правильный порт, после нажатия кнопки `Connection` вы увидите примерно следующее:

- на кнопке появится надпись `Connected`;
- будет показано название подключённого микроконтроллера, в этом примере `ATtiny1624`;
- название рядом с кнопкой — это автоматически определённое название микроконтроллера и его дескриптор, то есть код микросхемы, назначенный производителем;
- название рядом с кнопкой `Compile` — это параметр для компилятора.

![466](Pasted%20image%2020260726152346.png)

#### UART Observation Adapter Port Selection
Выбор порта адаптера для наблюдения UART

В другой вкладке браузера откройте раздел UART сайта **uartdebug.com** для наблюдения текста, формируемого `printf()`.

На рисунке показано, что сначала порт не выбран.

![668](Pasted%20image%2020260726152507.png)

После нажатия кнопки `Disconnected` появится окно выбора порта.

![355](Pasted%20image%2020260726152540.png)

Выберите порт второго адаптера, подключённого к передатчику UART0 микроконтроллера, и нажмите кнопку `Connection`.

Номер этого порта должен отличаться от номера порта адаптера UPDI. В этом примере адаптер UPDI использует `COM15`, а второй адаптер для наблюдения UART использует `COM24`.

Если всё работает правильно, вы увидите примерно следующее:

![501](Pasted%20image%2020260726152615.png)

Главный признак — надпись `Connected` на кнопке. Она означает, что подключение выполнено успешно.

Надпись `WCH CH343 ...` на вашем компьютере может отличаться. Она зависит от производителя адаптера USB-UART.

Параметры UART по умолчанию имеют правильные для этого проекта значения: `115200 8N1`.

Прошейте микроконтроллер этим проектом.

### Viewing the Output
Наблюдение выходных данных

В разделе UART сайта **uartdebug.com** используйте отображение принимаемых данных в режиме Text.

Вы должны увидеть строки примерно такого вида:

```text
Counter = 0
Counter = 1
Counter = 2
...
```

Новая строка передаётся приблизительно каждые 500 мс.

### Verification
Проверка

Если значение счётчика увеличивается каждые 500 мс, перенаправление `printf()` работает правильно.

Это проверяет:

- конфигурацию UART0;
- перенаправление `printf()` в `stdout`;
- передачу символов через USART0;
- вывод форматированного текста;
- аппаратное подключение к принимающему адаптеру USB-UART.

## What This Mini-Project Is For
Для чего предназначен этот мини-проект

Этот мини-проект демонстрирует использование `printf()` как простого программного средства отладки.

После перенаправления `stdout` в UART0 знакомая функция `printf()` позволяет выводить:

- значения переменных;
- счётчики;
- показания АЦП;
- результаты вычислений;
- сообщения о состоянии;
- сообщения об ошибках;
- диагностическую информацию.

Это удобнее, чем вручную преобразовывать каждое значение в отдельные байты UART.

## Usage Options
Варианты использования

Можно:

- выводить несколько переменных в одной строке;
- отображать целые значения в десятичном или шестнадцатеричном виде;
- заменить сообщение со счётчиком собственным диагностическим текстом;
- изменить интервал передачи 500 мс;
- выбрать другую поддерживаемую стандартную скорость передачи; при её изменении также измените настройку UART на сайте **uartdebug.com**;
- использовать UART1 вместо UART0; для этого требуется изменить код и подключить второй адаптер к выводу передачи UART1;
- изменить частоту CPU, как показано в проекте 02; обновите `F_CPU` в этом проекте в соответствии с новой частотой.

## Common Mistakes
Распространённые ошибки

- Отсутствие перенаправления `stdout`.
- Подключение `TxD` микроконтроллера к `TxD` адаптера вместо `RxD`.
- Отсутствие общего `GND`.
- Использование разных уровней логического напряжения.
- Выбор неправильного COM-порта.
- Выбор одного и того же COM-порта для обоих адаптеров.
- Выбор неправильной скорости передачи.

## Code Description
Описание кода

### Main Compiler Header
Основной заголовочный файл компилятора

`#include <xc.h>` предоставляет определения регистров и битов конкретного устройства для XC8.

### Standard I/O Library
Стандартная библиотека ввода-вывода

```c
#include <stdio.h>
```

Этот заголовочный файл предоставляет стандартный тип `FILE` и объявление функции `printf()`.

### CPU Clock Definition
Определение частоты процессора

```c
#define F_CPU 3333333UL
```

`F_CPU` используется библиотекой задержек и при расчёте скорости UART.

При заводских настройках тактирования частота процессора составляет приблизительно 3,333 МГц.

Эта директива не настраивает аппаратную частоту.

### Delay Library
Библиотека задержек

```c
#include <util/delay.h>
```

Этот заголовочный файл предоставляет `_delay_ms()` и `_delay_us()`.

`F_CPU` должна быть определена до подключения этого заголовочного файла.

### User-Defined UART Parameters
Параметры UART, задаваемые пользователем

```c
#define BAUD_RATE 115200UL
#define CLK_PER   F_CPU
```

`BAUD_RATE` выбирается пользователем.

`CLK_PER` — частота периферийного тактирования, используемая при расчёте скорости. Здесь она равна `F_CPU`.

### USART Baud Register Calculation
Расчёт регистра скорости USART

```c
#define USART_BAUD_RATE \
    ((uint16_t)(((float)CLK_PER * 64.0 / (16.0 * (float)BAUD_RATE)) + 0.5))
```

Это выражение рассчитывает значение, записываемое в `USART0.BAUD`.

Вычисление с плавающей точкой и добавление `0.5` округляют результат до ближайшего целого.

### printf Character Output Function
Функция вывода символа для printf

```c
int USART_PrintChar(char c, FILE *stream)
```

Эта функция вызывается перенаправленным стандартным потоком вывода каждый раз, когда `printf()` передаёт символ.

Аргумент `stream` требуется интерфейсом стандартного потока. В этом примере функция больше его не использует.

### Data Register Empty Check
Проверка освобождения регистра данных

```c
while (!(USART0.STATUS & USART_DREIF_bm))
{
    ;
}
```

Этот цикл опроса ожидает, пока буфер передачи USART0 сможет принять следующий байт.

Поскольку передача блокирующая, выполнение программы остаётся здесь до готовности передатчика.

### Character Transmission
Передача символа

```c
USART0.TXDATAL = c;
```

Эта операция записывает один символ, сформированный `printf()`, в регистр данных передатчика USART0.

### printf Output Stream
Выходной поток printf

```c
FILE USART_stream = FDEV_SETUP_STREAM(USART_PrintChar, NULL, _FDEV_SETUP_WRITE);
```

Эта строка создаёт стандартный поток ввода-вывода только для записи, функцией вывода символа которого является `USART_PrintChar()`.

### USART0 Initialization
Инициализация USART0

Функция `USART_Init()` настраивает UART0 и перенаправляет `stdout`.

#### UART Frame Format
Формат кадра UART

Присваивание `USART0.CTRLC` выбирает асинхронный режим, отсутствие чётности, 8 бит данных и 1 стоповый бит: `8N1`.

#### USART Baud Rate Register
Регистр скорости USART

```c
USART0.BAUD = USART_BAUD_RATE;
```

Этот регистр настраивает генератор скорости USART. При текущей частоте тактирования и значении `BAUD_RATE` обеспечивается скорость приблизительно 115200 бод.

#### UART0 TxD Pin Configuration
Настройка вывода TxD UART0

```c
PORTB.DIRSET = PIN2_bm;
```

PB2 настраивается как выход передачи UART0.

Для микроконтроллеров tinyAVR 2-series в корпусах SOIC-14 сигнал `PB2` находится на выводе 7.

#### USART Transmitter Enable
Разрешение передатчика USART

```c
USART0.CTRLB = USART_TXEN_bm;
```

Эта операция разрешает передатчик USART0.

#### stdout Redirection
Перенаправление stdout

```c
stdout = &USART_stream;
```

Эта операция перенаправляет стандартный вывод в ранее созданный поток USART.

После этого вызовы `printf()` используют `USART_PrintChar()` для передачи своих символов.

### UART1 Initialization Note
Примечание об инициализации UART1

Для преобразования примера в UART1:

1. Замените `USART0.` на `USART1.`.
2. Замените `PORTB.DIRSET = PIN2_bm;` на `PORTA.DIRSET = PIN1_bm;`.

Для предполагаемых микроконтроллеров tinyAVR 2-series в корпусе SOIC-14 сигнал `TxD` UART1 находится на `PA1`, вывод 11.

### The Entry Point
Точка входа

`int main(void)` — точка входа программы.

### Transmission Counter Variable
Переменная счётчика передачи

```c
uint16_t tr_count = 0;
```

Эта переменная хранит значение счётчика, выводимого функцией `printf()`.

### USART0 Initialization Call
Вызов инициализации USART0

```c
USART_Init();
```

UART0 настраивается, а `stdout` перенаправляется до запуска главного цикла.

### Main Infinite Loop
Главный бесконечный цикл

Цикл `while (1)` непрерывно выводит и увеличивает счётчик.

### Formatted Text Transmission
Передача форматированного текста

```c
printf("Counter = %u\r\n", tr_count);
```

Эта функция форматирует текущее значение `tr_count` как беззнаковое десятичное число и передаёт полученные символы через перенаправленный поток `stdout`.

`\r\n` завершает строку текста.

### Counter Increment
Увеличение счётчика

```c
tr_count++;
```

Эта операция увеличивает счётчик после каждой переданной строки.

### Transmission Interval
Интервал передачи

```c
_delay_ms(500);
```

Эта функция создаёт интервал приблизительно 500 мс между выводимыми строками и блокирует главный цикл во время задержки.

### Application Scope
Область применения

Этот мини-проект предназначен для микроконтроллеров tinyAVR 2-series с совместимыми регистрами USART и маршрутизации выводов, а также реализацией стандартного ввода-вывода XC8, поддерживающей `FDEV_SETUP_STREAM`.

### Tested Hardware
Проверенное оборудование

- `ATtiny1624`, SOIC-14
